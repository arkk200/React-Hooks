import { useTitle } from "@arkkhooks/use-title/useTitle.hook";

function ArkkHooksUseTitle() {
  useTitle("Nice title");
  return <div>Custom useTitle Hook</div>;
}

export default ArkkHooksUseTitle;
